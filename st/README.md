# my simple st (simple terminal) module
This works via turning the NixOS config set into a config.def.c.
This is a package override,
take that as you will it's a warning in the case of something breaking.

# how this works
This attempts to work for both people who use flakes,
and for those who don't.
